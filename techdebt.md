# Tech Debt — Bugs & Gaps

Audit date: 2026-07-19 (branch `jobric_011`, at commit `054c870`).

Findings from a full read of the agents Worker, web app, D1 migrations, and CI.
Severity reflects impact on the product working end-to-end, not effort to fix.

## Summary

The ingestion pipeline and the UI were built as two disconnected halves. The
pipeline parses on metadata it doesn't actually have, and the dashboard displays
data it never fetches — so end-to-end, no real email currently reaches a user's
screen. Fix #1 and #2 first, since they determine whether the rest is even
observable.

## Findings

| #   | Issue                                                                                                                                                                                                                                                                                                                                                      | Location                                                                   | Severity     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------ |
| 1   | **Dashboard renders 100% hardcoded mock data.** `_data/companies.ts`, `inbox.ts`, `overview.ts` are static arrays ("Northwind Design", "47 applications"). Nothing in the web app ever calls the orchestrator's `/applications` endpoint — the only `AGENTS_URL` fetches are the two email-account routes. The pipeline writes to D1 and nothing reads it. | `apps/web/src/app/dashboard/_data/`                                        | **Critical** |
| 2   | **Parser never sees an email body.** Gmail fetch uses `format=metadata` (no body), and neither ingestion path sets `envelope.body`. The prompt always hits its `'(body not available)'` fallback, so extraction runs on subject + sender only. `snippet` is fetched but never forwarded.                                                                   | `apps/agents/src/agents/parser.ts:37`, `apps/agents/src/cron.ts:81`        | **Critical** |
| 3   | **Claimed dedup does not exist.** `cron.ts:12` says duplicates are "absorbed by the watcher's `UNIQUE(user_id, gmail_message_id)` constraint" — the watcher has no dedup and never writes to `messages`. Any mid-batch failure replays the whole batch: duplicate LLM spend, duplicate `events` rows.                                                      | `apps/agents/src/agents/email-watcher.ts`                                  | **Critical** |
| 4   | **Expired `historyId` bricks an account permanently.** Google expires `startHistoryId` after ~a week; `listHistory` then throws 404 forever and `last_history_id` is never re-anchored. No 404 recovery path.                                                                                                                                              | `apps/agents/src/gmail/client.ts:91`                                       | **Critical** |
| 5   | **Both DOs are global singletons** — `idFromName('watcher')` / `idFromName('main')` for _all_ users. Every email in the system serializes through one Durable Object doing multi-second LLM calls. Hard throughput ceiling + one user's backlog stalls everyone.                                                                                           | `apps/agents/src/cron.ts:75`, `apps/agents/src/agents/email-watcher.ts:29` | **Critical** |
| 6   | **Status can silently regress.** Orchestrator applies `newStatus` unconditionally; an out-of-order email downgrades `offer` → `applied` and rewrites `funnel_rank`. No monotonic guard.                                                                                                                                                                    | `apps/agents/src/agents/orchestrator.ts:87-98`                             | High         |
| 7   | **`threads` and `messages` tables are never written.** No thread grouping, `events.message_id` is hardcoded `NULL`, so the Inbox feature has no data source even once #1 is fixed.                                                                                                                                                                         | schema vs. `apps/agents/src/agents/orchestrator.ts`                        | High         |
| 8   | **Gmail connect likely 500s on a fresh user.** `email_accounts.user_id` has an FK to `users`, D1 enforces FKs, and that row only exists if the Clerk webhook fired. Undocumented ordering dependency.                                                                                                                                                      | `apps/agents/src/routes/email-accounts.ts:61`                              | High         |
| 9   | **CORS is broken.** Preflight returns `Access-Control-Allow-Origin: *`, but no actual response carries CORS headers — browser calls fail after a successful preflight.                                                                                                                                                                                     | `apps/agents/src/index.ts:29-37`                                           | High         |
| 10  | **SETUP.md omits half the required secrets.** Documents only `CLERK_SECRET_KEY` and `ANTHROPIC_API_KEY`. `GMAIL_TOKEN_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `CLERK_WEBHOOK_SECRET` are all read at runtime and undocumented there — the cron throws on first run.                                                                              | `apps/agents/SETUP.md`                                                     | High         |
| 11  | **Cron has no batch cap.** Every new message triggers 2 sequential LLM calls inside one cron tick — a backlog blows Workers' subrequest/CPU limits, and the watermark never advances.                                                                                                                                                                      | `apps/agents/src/cron.ts:79`                                               | Medium       |
| 12  | **Keyword filter is very leaky.** Matches on `from` too, so every `linkedin` notification passes; `role`/`job`/`career` catch most newsletters. Real ATS mail without those words is silently dropped.                                                                                                                                                     | `apps/agents/src/agents/email-watcher.ts:51`                               | Medium       |
| 13  | **`parse_failures` is write-only.** Rows are captured for "later replay" but no replay path or admin view exists.                                                                                                                                                                                                                                          | `apps/agents/src/agents/orchestrator.ts:153`                               | Medium       |
| 14  | **Email Workers ingress path is dead code.** `email()` looks up `email_accounts` by `message.to`, but those rows hold Gmail addresses — Cloudflare only delivers mail for CF-managed domains. Unreachable in the current product model.                                                                                                                    | `apps/agents/src/index.ts:89`                                              | Medium       |
| 15  | **Fake user fallback in production.** Missing Clerk user renders as "Mira K. / mira@gmail.com" rather than an error state.                                                                                                                                                                                                                                 | `apps/web/src/app/dashboard/page.tsx:7-11`                                 | Low          |
| 16  | **Stale build artifacts committed.** `packages/shared/dist/gmail/*` has no corresponding `src/gmail/`.                                                                                                                                                                                                                                                     | `packages/shared/dist/`                                                    | Low          |
| 17  | **Docs drift.** CLAUDE.md says "no test runner configured"; vitest is installed and CI runs `pnpm test`. Coverage is one trivial `formatJobTitle` test — zero tests on the pipeline, crypto, or OAuth.                                                                                                                                                     | `CLAUDE.md`                                                                | Low          |
| 18  | **`apps/mobile` is an empty shell** — a `package.json` and one dependency, no source.                                                                                                                                                                                                                                                                      | `apps/mobile/`                                                             | Low          |
