import type { ViewKey } from '../_lib/types'

export const TITLES: Record<ViewKey, { h: string; s: string }> = {
  overview: { h: 'Overview', s: '' },
  inbox: { h: 'AI Inbox', s: '' },
  applications: { h: 'Applications', s: '' },
}
