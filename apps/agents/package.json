{
  "name": "@jobric/agents",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check-types": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply jobric-db --local",
    "migrate:prod": "wrangler d1 migrations apply jobric-db"
  },
  "dependencies": {
    "@clerk/backend": "^1.24.0",
    "agents": "^0.3.0",
    "ai": "^6.0.0",
    "@ai-sdk/anthropic": "^3.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
