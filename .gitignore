# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.dev.vars

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Cloudflare
.wrangler/

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Agent skills (installed locally via `npx skills add`; third-party content,
# pinned in skills-lock.json — kept out of the repo deliberately)
.agents/
.claude/skills/
skills-lock.json

# Agent worktrees (throwaway checkouts created by Claude Code; a full copy of
# the repo lives here, so leaving it tracked also breaks repo-wide tooling globs)
.claude/worktrees/
